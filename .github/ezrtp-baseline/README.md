# EzRTP Config Baseline

This directory holds copies of EzRTP's default config files extracted from the last
release that was checked by the compatibility workflow.

Files here are updated automatically when a new EzRTP release is processed.
If the workflow opens a GitHub Issue, the diff shown there is against these files.

Do not edit these files manually — they are owned by the compatibility check workflow.
